type Message {
  content: String!
  author: String!
}

type Query {
  messages(): [Message!]!
}

type Mutation {
  sendMessage(content: String!, author: String!): Message!
}

type Subscription {
  messageSent: Message!
}
